# 人物属性与自由加点系统设计（定稿）

> 来源：用户 2026-08-12 提出框架，2026-08-13 确认全部派生公式与战斗规则。
> 状态：已全部接入游戏（派生公式、自由加点、精神攻防、先攻多段、挂机修炼）。

---

## 1. 六项可加点属性

| 属性 | 决定 / 机制 |
| -- | -- |
| **体质** | 物理防御力 + 生命值；战斗中损耗生命值，休息/离线结算时回满。 |
| **力量** | 物理攻击力。 |
| **灵魂** | 精神攻击力 + 精神防御力。 |
| **速度** | 闪避率 + 先攻值。<br>先攻值多段规则（比值 = 我方先攻 / 对手先攻）：<br>· 2× → 当回合先行动 **2 次**　· 4× → **3 次**　· 6× → **4 次**　· 8× → **5 次**　· 10× → **6 次**<br>· 极品特殊装备可额外增加「本回合行动次数」（`extraActions` 字段）。 |
| **悟性** | 功法学习速度 + 灵力值；功法攻击消耗灵力；提高挂机经验加成。 |
| **天命** | 幸运值；提高闪避率 + 开宝箱出极品概率；提高挂机经验加成。 |

> 可分配点数 = **境界小阶数 × 10 − 已分配**（`POINTS_PER_STAGE = 10`）。
> 单项属性**不设上限**（仅下限 = 初值 10，用于回退加点）。后期做「洗点」重置。

---

## 2. 派生属性公式（已定稿，用户给定）

基础属性初值均为 10，境界「小阶序号」记为 `idx`（= `CULTIVATION.realmFromXp(xp).globalIndex`，炼气一重天为 0）。

| 派生 | 公式 | 说明 |
| -- | -- | -- |
| 生命上限 maxHp | `100 + 体质×20 + idx×100` | 体质主导 |
| 物理防御 def | `10 + 体质×2 + idx×2.5` | 体质主导 |
| 物理攻击 atk | `20 + 力量×2 + idx×8` | 力量主导 |
| 灵力上限 maxMp | `100 + 悟性×20 + idx×100` | 悟性主导（灵力=放功法消耗） |
| 精神攻击 spiAtk | `20 + 灵魂×2 + idx×10` | 灵魂主导 |
| 精神防御 spiDef | `10 + 灵魂×1 + idx×2` | 灵魂主导 |
| 先攻值 init | `10 + 速度×2 + idx×2` | 速度主导 + 境界加成 |
| 闪避率 eva | `min(95%, 10% + idx×0.1% + 速度×0.1% + 天命×0.1%)` | 速度+天命+境界 |
| 幸运 luck | `= 天命` | 影响宝箱极品概率 |
| 挂机经验加成 xpBonus | `悟性×0.2% + 天命×0.2%` | 悟性+天命，比例叠加 |

> 默认角色（全 10，idx=0）示例：maxHp 300 / def 30 / atk 40 / maxMp 300 / spiAtk 40 / spiDef 20 / init 30 / 闪避 12% / 挂机加成 4%。

---

## 2.5 境界经验曲线（递增，已接入 cultivation.js）

> 需求：升级突破**至少玩一年才到顶**，且**每升一阶所需经验随境界递增**（越往后越久）。
> 当前共 **168 个小阶**（注：用户原称"162 小阶"，按境界表实际实现为 168）。

- 每升一小阶所需修为：`req(i) = BASE + STEP·i + QUAD·i²`（i = 当前小阶序号，0 起）
- 参数（cultivation.js 顶部 `STAGE_XP_*`）：`BASE=200, STEP=20, QUAD=24`
- `realmFromXp(xp)` 用「累计阈值 + 二分查找」：预计算 `CUM[i]` = 进入第 i 小阶所需总修为，再反查 idx / 进度。
- 调参：抬高 `BASE` 整体变难；`STEP` 控制线性斜率；`QUAD`（二次项）主导后期拉长——这是"至少一年"的关键。
- 实测曲线（部分节点，在线 2 修为/秒）：

| 小阶 idx | 本阶所需修为 | 累计修为 | 在线 2/s 估算耗时 |
|---|---|---|---|
| 0（炼气一重天） | 200 | 200 | ~2 分钟 |
| 10 | 2,800 | 12,540 | ~23 分钟 |
| 50 | 61,200 | 1,065,900 | ~8.5 小时 |
| 100 | 242,200 | 8,241,600 | ~33.5 小时 |
| 167（宇宙国主圆满） | 672,876 | 37,236,324 | ~93 小时/阶 |

- 满级总修为 ≈ **3,724 万**（进最后一阶）/ 3,791 万（含最后一阶）。
- 按挂机节奏（在线 2/s、离线 1/s 封顶 12h/天）到满级天数：
  - 肝帝（每天在线 8h）≈ **369 天 ≈ 1.01 年**
  - 重度（每天在线 2h）≈ 646 天 ≈ 1.77 年
  - 休闲（每天在线 1h）≈ 739 天 ≈ 2.02 年
  - 纯离线挂机 ≈ 862 天 ≈ 2.36 年
- 结论：前期第 1 阶就要 ~2 分钟（绝不"几秒一级"），后期单阶需数十~上百小时在线，整体**至少玩一年**才到顶，符合需求。

---

## 3. 战斗规则（已定稿）

- **单一血量轴**：普攻与功法都只扣「生命值」，生命归零即败。不设独立精神血量。
- **攻击类型**（由 `damage(attacker, target, mult, type)` 区分）：
  - 普攻 / 物理功法 → 用 `atk` 打 `def`。
  - 精神功法 → 用 `spiAtk` 打 `spiDef`。
  - 两者都扣同一 `hp`。
- **功法表 `SKILLS`**（灵力消耗各由功法而定，非固定）：
  - 旋风剑法 `xuanfeng`：物理，倍率 ×1.8，耗灵 12。
  - 玄阴指 `xuanyin`：精神，倍率 ×1.6，耗灵 15。
  - 玩家 `learned` 默认习得两者，`activeSkill` 为出战功法（在「功法」菜单切换）。
  - 敌人使用自身 `skill`（物理，耗灵 10）。
- **速度多段先攻** `beginRound()`：比值 ≥2× 时快者连动 `floor(比值/2)+1` 次（即 2×→2、4×→3、6×→4、8×→5、10×→6），再加 `extraActions`（装备）。
- **闪避**：`随机 < 目标闪避率` 则未命中（显示「闪避」）。

---

## 4. 挂机修炼系统（已定稿 + 已接入）

- **在线**：停留在游戏画面（主页 / 地图 / 战斗）即自动累加修为，速率 `ONLINE_XP_PER_SEC = 2/秒`，再乘 `(1 + 挂机加成)`。每 5 秒落盘一次。
- **离线**：按离开时长结算，速率 `OFFLINE_XP_PER_SEC = 1/秒`（约为在线一半），封顶 `OFFLINE_CAP_SEC = 12 小时`。进入游戏时弹「离线挂机结算」提示并回满气血。
- 实现：`player.lastSeen` 记录时间戳；`gainXp(amount, fullHeal)` 累加并重算派生；`applyOfflineXp()` 读取 `lastSeen` 结算。
- 悟性 / 天命越高，挂机经验加成比例越大（`xpBonus`）。

---

## 5. 宝箱 / 幸运系统（设计已确认，待实现）

- **触发来源**：剧情副本、活动副本、商城开宝箱（这些战斗/入口尚未实装）。
- **天命作用**：提高「开宝箱出极品概率」。机制预留 `luck = 天命`，后续宝箱抽取按 `luck` 提升极品权重。
- **状态**：仅设计确认，未写代码（依赖剧情/活动副本与商城系统）。

---

## 6. 已接入代码位置（index.html）

- 玩家对象：`con/str/sou/spd/com/des` + `spent` + `extraActions` + `xpBonus` + `learned` + `activeSkill` + `lastSeen`。
- `recalcStats(player)`：第 2 节公式。
- `SKILLS` 表 + `showSkillsModal()`（功法菜单，可切出战功法）。
- `showAttrModal()`：6 属性 `[−][+]` 加点 + 实时派生（含先攻值、闪避率、挂机加成）。
- 战斗：`beginRound()`（多段先攻）、`damage(type)`（精神/物理）、`applyAction`/`enemyAct`（按功法消耗）。
- 挂机：`gainXp` / `applyOfflineXp` / `initHub` 内 `setInterval`。

---

## 7. 性能优化记录（2026-08-13）

- **现象**：主页进入后非常卡（GitHub Pages 国内访问 + 主页常驻渲染）。
- **根因 1**：`render()` 是 60fps 的 `requestAnimationFrame` 死循环，主页时仍每帧清空画布 → 强迫整页每帧重排合成，与视频解码抢主线程。已改为在 `state==='hub' || 'create'` 时直接跳过绘制（DOM 层不需要画布）。
- **根因 2**：M2 视频原文件 7.1MB / 7084kbps，国内加载慢、缓冲卡顿。已用 ffmpeg 转码为 H.264 / 720p / 去音轨 / ~294KB（≈原 1/24），并加 `?v=2` 缓存破坏。原文件存 `assets/char/char_m2_orig.mp4` 备份。
- **效果**：页面秒开、视频即时播放、主页不再空转重绘。

- 存档：6 属性、`spent`、`learned`、`activeSkill`、`lastSeen` 全部持久化。

## 7. 落地待办

- [x] 派生公式（用户定稿）
- [x] 可分配点数 ×10
- [x] 属性加点界面 + 全局弹窗
- [x] 精神攻防（物理/精神双类型，单血量轴）
- [x] 功法表 + 各自灵力消耗 + 功法菜单出战切换
- [x] 先攻多段 2/4/6/8/10× + 装备 extraActions 钩子
- [x] 挂机修炼（在线 tick + 离线结算）
- [ ] 宝箱/幸运系统（依赖剧情/活动副本、商城）
- [ ] 洗点功能（后期）
- [ ] 极品装备系统（设置 extraActions / 附加属性）
